const { Recipe, Diet } = require("../db.js");
const axios = require("axios");

const createRecipe = async (req, res) => {
  try {
    
   
  } catch (error) {
    console.error(`Missing parameters 🕵️‍♂️`);
  }
};

module.exports = {
  createRecipe
};